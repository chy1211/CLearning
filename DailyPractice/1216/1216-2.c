#include <stdio.h>
#include <stdlib.h>
int main()
{
	int a,c;
	float b;
	printf("請輸入存入a元利率是b,如果每年的利息都繼續存入銀行,即複利,將輸出幾年後本金+利息可超過c元");
	scanf("%d",a);
	scanf("%f",b);
	scanf("%d",c);
	printf("%d,%f,%d"a,b,c);
}
